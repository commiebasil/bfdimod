on_actions = {
	on_startup = {
		effect = {
			if = {
				limit = { EMP = { is_ai = no } }
				GOI = { change_tag_from = EMP }
			}
			set_global_flag = showwatermark
			set_global_flag = welcome
		}
	}
}